---
import { readingTime } from "../lib/reading-time";

type Props = {
  class?: string;
  label?: string;
  text: string;
};

const { class: className = "", label = "min read", text } = Astro.props;
const { minutes } = readingTime(text);
---
<span class={className}>{minutes} {label}</span>
